# Hue experiments

Make sure to press the button on the hue bridge before executing the scripts.
The library will automatically create a user for you.

To run the experimnents execute:

```
bundle install
ruby test.rb
```